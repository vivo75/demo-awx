---
# ansible-playbook -v -iLABgitea, install-db.yml

- hosts: all
  gather_facts: no
  become: true
  vars_files:
    - vars.yml
  tasks:
    - name: Update system
      ansible.builtin.apt:
        name: "*"
        state: latest
        update_cache: yes

- hosts: all
  gather_facts: yes
  become: true
  vars_files:
    - vars.yml

  roles:
    - ansible-role-postgresql
